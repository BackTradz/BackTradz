# backend/models/response_model.py
# =================================
# Ce fichier est prévu pour contenir les modèles Pydantic
# qui définissent les schémas de réponse de l'API (FastAPI).
# Exemple d'utilisation :
#
# from pydantic import BaseModel
#
# class BacktestResponse(BaseModel):
#     message: str
#     credits_remaining: int
#     csv_result: str
#     xlsx_result: str
#
# Cela permet :
# - d'avoir une documentation Swagger propre
# - d'assurer que l'API retourne toujours des structures cohérentes
#
# ⚠️ Actuellement ce fichier ne contient qu’un commentaire,
# mais il est prêt à être rempli avec des modèles concrets.
# Modèles Pydantic de réponse
